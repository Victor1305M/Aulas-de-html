#include<stdio.h>
#include<locale.h>

   int main()
   {
   	int num,cont,resul:
   		setlocale(LC ALL,"Portuguese");
   		printf("\n Informe o número\n");
   		scanf("%d,&num");
   		
   		if(num>=1 && num<=10) /*se num
   		>=1 e num<=10*/
   		{
   			for(cont=1;cont<=,cont++)
   		}
   		resul
   }
